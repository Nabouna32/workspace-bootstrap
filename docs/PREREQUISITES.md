# Prerequisites

Workspace Control targets supported **Windows 11 x64** systems.

The engine may inspect Windows version/build, architecture, memory/storage, administrator capability, Windows components, package providers, WSL capability, driver state and application prerequisites.

Prerequisites are capability-specific. Missing prerequisites produce actionable diagnostics and never silently substitute incompatible versions or sources.