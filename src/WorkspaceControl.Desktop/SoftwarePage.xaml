<Page
    x:Class="WorkspaceControl.Desktop.SoftwarePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Spacing="4">
            <TextBlock x:Uid="SoftwarePage_Title" FontSize="32" FontWeight="SemiBold" />
            <TextBlock x:Uid="SoftwarePage_Subtitle" Opacity="0.72" />
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="12" Margin="0,20,0,12">
            <Button x:Uid="SoftwarePage_Refresh" Click="Refresh_Click" />
            <ProgressRing x:Uid="SoftwarePage_ProgressRing" IsActive="{Binding IsBusy}" Width="20" Height="20" />
        </StackPanel>

        <ListView Grid.Row="2" x:Uid="SoftwarePage_List" ItemsSource="{Binding SoftwareItems}" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="8" ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding Name}" FontWeight="SemiBold" />
                        <TextBlock Grid.Column="1" Text="{Binding Version}" Opacity="0.72" />
                        <TextBlock Grid.Column="2" Text="{Binding Publisher}" Opacity="0.72" TextTrimming="CharacterEllipsis" />
                        <TextBlock Grid.Column="3" Text="{Binding Ownership}" Opacity="0.72" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
