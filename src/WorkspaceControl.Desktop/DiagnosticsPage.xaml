<Page
    x:Class="WorkspaceControl.Desktop.DiagnosticsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Spacing="4">
            <TextBlock x:Uid="DiagnosticsPage_Title" FontSize="32" FontWeight="SemiBold" />
            <TextBlock x:Uid="DiagnosticsPage_Subtitle" Opacity="0.72" />
        </StackPanel>

        <ListView Grid.Row="2"
            x:Uid="DiagnosticsPage_List" ItemsSource="{Binding Diagnostics}" SelectionMode="None" />
    </Grid>
</Page>
