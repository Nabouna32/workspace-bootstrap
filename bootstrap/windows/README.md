# Workspace Control — Windows resources

This directory currently contains the legacy bootstrap resource layout being migrated into the Workspace Control catalog.

The target model is a catalog of capabilities, applications, policies, optimizations and profiles rather than a bootstrap-only component list.

New resources must follow Workspace Control contracts. Applications are provider-neutral. Profiles describe desired state. Optimizations declare risk and reversibility. Loading a resource must never mutate the system.